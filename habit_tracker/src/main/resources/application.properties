# ? Use environment variables ? never commit real credentials to git
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/habit_tracker}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:Ssc@070203}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ? JWT secret from env var ? must be at least 32 chars
jwt.secret=${JWT_SECRET:myhabittrackersecretkey1234567890abcdefghij}